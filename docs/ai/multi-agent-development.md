# 多 Agent 协同开发规范

> 状态：项目级协作流程权威文档  
> 原则：单一用户入口、动态委派、只读优先、默认单写入、独立验证、人工 Git 门禁

本文档是 Agent 角色、权限和协作流程的规范源；`.codex/config.toml` 和
`.codex/agents/*.toml` 是 Codex 运行时执行投影。修改角色或权限时必须同步更新两者，
并运行 `scripts/validate-agent-governance.ps1` 检查关键不变量。

## 1. 角色模型

用户只与当前主 Agent 沟通。主 Agent 是职责，不是永久聊天窗口，负责：

- 澄清需求、目标、非目标和验收标准。
- 读取项目规则并检查当前工作树。
- 判断任务规模、风险和需要的子 Agent。
- 汇总调查结果并提出架构与 RIO。
- 等待用户批准关键决策。
- 冻结任务包、分配文件所有权和停止条件。
- 集成实际修改、组织验证和更新任务记录。
- 在人工 Git 门禁前停止。

项目提供四个基础子 Agent：

| 角色 | 默认权限 | 使用时机 |
| --- | --- | --- |
| code-explorer | 只读 | 调查入口、调用链、数据流、测试和复用点 |
| architecture-reviewer | 只读 | 审查边界、契约、RIO、迁移和兼容 |
| implementation-worker | 工作区写入 | 实现已经批准且边界明确的工作包 |
| validation-reviewer | 只读 | 独立检查最终 diff、测试和回归风险 |

安全、数据库、基础设施、性能、浏览器复现等专家按需临时增加。领域知识优先写入
架构文档，不持续堆入基础 Agent 配置。

## 2. 任务分级

### 小型任务

特征：范围清晰、低风险、不改变公共契约、通常只涉及少量文件。

流程：

~~~text
主 Agent 调查 → 实现 → 针对性验证 → 交付
~~~

默认不创建子 Agent。

### 中型任务

特征：涉及一个完整调用链或前后端中的多个文件，但架构边界稳定。

流程：

~~~text
建立任务档案 → 只读调查 → 主 Agent 确认方案 → 单个实现 Agent → 主 Agent/独立验证
~~~

### 大型或高风险任务

特征：跨前后端、数据库、权限、基础设施或领域边界，或者包含迁移与兼容要求。

流程：

~~~mermaid
flowchart TD
  U["用户提出需求"]
  M["主 Agent 建档并明确目标与非目标"]
  E["代码调查 Agent"]
  A["架构审查 Agent"]
  R["必要的官方资料/公开库调研"]
  D["主 Agent 汇总方案与 RIO"]
  C["用户批准"]
  P["冻结任务包和契约"]
  W["默认一个实现 Agent"]
  V["独立验证 Agent"]
  I["主 Agent 集成检查"]
  DOC["更新中文交付记录"]
  G["人工 Git 门禁"]

  U --> M
  M --> E
  M --> A
  M --> R
  E --> D
  A --> D
  R --> D
  D --> C
  C --> P
  P --> W
  W --> V
  V --> I
  I --> DOC
  DOC --> G
~~~

## 3. 调研与 RIO

中大型任务按以下顺序调查：

1. 检查工作树、项目规则和任务记录。
2. 搜索仓库内已有实现、依赖、测试和约定。
3. 确认仍存在知识缺口后，查询官方文档。
4. 必要时比较最多三个严肃的公开库或方案。
5. 对不确定的关键假设进行有时间边界的 Spike。
6. 输出架构方案和 RIO，等待用户批准关键选择。

公开库评估至少考虑兼容性、维护状态、安全、许可证、传递依赖、API 稳定性、
运维成本和退出策略。禁止未经用户批准引入新的生产依赖。

本项目 RIO 定义：

- Risks：失败模式、安全风险、技术债和未知项。
- Impact：对接口、数据、权限、性能、部署、兼容和用户体验的影响。
- Options：可选方案、取舍、推荐方案和回退路径。

## 4. 子 Agent 任务包

主 Agent 委派时只提供完成工作所需的最小上下文：

~~~text
任务编号：
任务目标：
非目标：
验收标准：
必读文档：
相关文件和入口：
已批准决策：
文件所有权：
允许的操作：
禁止的操作：
停止并上报条件：
返回格式：
~~~

停止并上报条件至少包括：

- 需求与架构或当前代码逻辑冲突。
- 必须修改未分配文件或公共契约。
- 发现用户/其他 Agent 的重叠修改。
- 测试失败原因超出工作包。
- 需要新依赖、迁移、权限或外部系统变更。

## 5. 并行与写入规则

- 默认最多同时运行三个子 Agent，日常优先使用一到两个。
- 优先并行代码调查、文档核实、测试分析和独立审查。
- 默认只有一个 Agent 写代码。
- 委派、独立 worktree 或上下文切换前，主 Agent 必须先在
  docs/changes/active-tasks.json 登记任务、工作树、文件范围和共享资源。
- 活动登记由中央主 Agent 单点、串行维护；子 Agent 和实现 Agent 不得修改中央登记。
- 主 Agent 维护中央 delivery.md，子 Agent 写各自独立的 handoffs/*.md 或返回摘要。
- 多 Agent 不得同时修改同一个文件。
- 并行写入前必须冻结接口契约，并为每个 Agent 分配明确文件所有权。
- 大型独立写入使用不同 Git worktree；一个 worktree 对应一个分支和一个顶层 Codex 任务。
- .env.video.local 等被忽略的密钥文件不得自动复制到 worktree。
- 共享 Docker、数据库、端口和本地服务的最终集成验证必须串行执行。
- 同一工作树同一时间只能登记一个活动任务；大型独立写入必须使用不同 worktree。
- 文件范围使用仓库相对路径或目录前缀，不使用通配符。两个活动任务的范围相同或存在
  父子包含关系时视为冲突，必须重新拆分或串行执行。
- 共享资源使用稳定标识，例如 `docker:video-platform`、`db:local-postgres`、
  `port:8080`。相同资源不能同时被两个活动任务独占登记。

## 6. 任务记录

中大型任务，以及任何实际使用子 Agent、独立 worktree、交接或上下文切换的任务使用：

~~~text
docs/changes/<任务编号>/
  delivery.md
  decisions.md
  verification.md
  handoffs/
~~~

活动任务还必须登记到：

~~~text
docs/changes/active-tasks.json
~~~

中央登记只保存当前活动任务，至少包含：

- `taskId`、名称、状态和当前主 Agent。
- 任务目录和工作树。
- 互不重叠的仓库相对文件范围。
- 独占的共享资源标识。
- 最后更新日期。

任务状态固定为：`调研中`、`待决策`、`已批准`、`实现中`、`验证中`、
`待人工提交`。完成或取消后从活动登记删除，但任务档案长期保留，并在 delivery.md
记录最终状态和重要提交引用。

- delivery.md：目标、状态、进度、修改范围、风险和下一步。
- decisions.md：用户批准的决策、备选方案、理由和影响。
- verification.md：已经实际执行的测试、结果和未验证项。
- handoffs/：子 Agent 的精简中文交接。

中央文档由主 Agent 单点维护。Git 保存实际提交历史，文档只保留重要提交引用，
不复制完整提交日志。

中央登记和任务档案的更新时机：

1. 启动受管任务时先建档、登记，再委派或写入。
2. 用户批准关键决策后更新 decisions.md 和任务状态。
3. 文件范围、工作树或共享资源变化时先更新登记，再继续操作。
4. 上下文切换前更新已完成、未完成、风险和下一步。
5. 验证完成后更新 verification.md，并将状态改为待人工提交。
6. 用户完成提交或明确取消后，填写任务最终状态并从活动登记移除。

## 7. 上下文切换

主 Agent 是角色，不要求整个项目永久使用同一个对话。出现以下情况时建立检查点并
开启新的 Codex 任务：

- 一个阶段或独立功能已经完成。
- 对话积累大量日志、失败尝试或无关历史。
- 需求方向发生明显变化。
- Agent 开始重复、遗忘或混淆已批准决策。
- 准备从设计进入实现，或从实现进入独立集成验证。

切换前由旧主 Agent 更新任务档案，至少写清：

1. 已完成
2. 未完成
3. 当前风险
4. 下一步

新主 Agent 必须重新读取正式文档并检查当前 git status 和 git diff，不能盲信
旧摘要。还必须核对 docs/changes/active-tasks.json 与当前任务档案是否同步。长期连续性
由仓库文档保障，不依赖聊天历史。

## 8. 验证与交付

验证 Agent 必须以批准的验收标准、权威架构、实际 diff 和当前测试为依据。问题等级：

- P0：安全、数据破坏、严重不可用，阻塞交付。
- P1：明确功能错误、权限漏洞、重大回归，阻塞交付。
- P2：重要但有规避方式的风险，应修复或由用户接受。
- P3：低风险改进，不阻塞交付。

最终交付必须包含：

- 修改文件和行为摘要。
- 已执行测试及真实结果。
- 未执行测试及原因。
- 残余风险和已知限制。
- 建议提交拆分和 commit message。
- 明确说明 AI 未执行暂存、提交、推送或 GitHub 操作。

修改协作规范、任务模板、中央登记或 `.codex/agents/*.toml` 时，还必须执行：

~~~powershell
powershell -ExecutionPolicy Bypass -File scripts/validate-agent-governance.ps1
~~~

该命令检查活动任务档案、状态同步、工作树唯一性、文件范围和共享资源冲突，以及四个
基础 Agent 的关键权限。它可以被本地流程或任意 CI 调用，但不替代人工语义审查。

## 9. 人工 Git 门禁

任何 Agent 都不能执行 Git 写操作或 GitHub 发布操作。主 Agent 只能在验证完成后向
用户提供建议，由用户本人检查 diff、暂存、提交、推送、创建和合并 PR。

仓库提供治理校验脚本，但默认不自动修改用户 Git Hook 或 Git 配置。如果未来需要更强
的机械保证，可由用户将脚本接入实际 CI，或增加项目 Hook/受管规则拦截 Git 写命令；
启用前应单独验证其对只读 Git 命令、构建工具和人工终端的影响。
