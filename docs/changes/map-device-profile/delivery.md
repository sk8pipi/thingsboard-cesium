# 按设备配置统一点位展示

- 任务编号：map-device-profile
- 当前状态：待人工提交
- 当前主 Agent：主 Agent
- 最后更新：2026-09-12

## 目标
用户已批准完整方案并要求实现：设备配置 ID 统一驱动用户大屏、编辑预览、详情类型、统计分类和点位样式；摄像头进入统一样式面板；复用配置图片；自动按设备配置选择数据/视频点位；旧模板预览迁移并在保存时落地，保留回退快照。

## 工作包与文件所有权
前端地图、业务聚合、对应测试；后端 MapTemplateRuntimeService 与专项测试；领域文档。保持设备 UUID、坐标/模型锚点、客户分配和视频接口边界。无数据库迁移、无生产依赖、无实际设备批量写入。主 Agent 独占前端与文档；runtime_implementation 独占 MapTemplateRuntimeService.java 和 MapTemplateRuntimeServiceTest.java 两个后端文件，契约已冻结且与前端文件不重叠，可独立验证。其余子 Agent 只读调查与验证。

## 当前进度
已实现受保护 runtime 配置元数据、配置 ID 样式继承、原生图片缓存、详情与统计统一分类、新选点自动分类、旧规则冲突处理与保存时备份。前端 7 个专项脚本和后端 7 项专项测试通过，代码检查结果见 verification.md。补齐领域架构及索引。

主 Agent 复核并修复旧 entityMetadata 遮盖新配置、详情编辑操作未随类型更新、旧分类包装值迁移、同名不同 ID 的统计 key、类型预览误继承单点覆盖。实现未操作用户实际模板或设备。

## 验收标准
分类 ID/名称与遥测隔离；配置改名、换配置、default/未知、图片失败；单点/配置继承；用户和编辑详情一致；旧样式冲突与回退；摄像头能力不受图标改变影响；坐标/模型/排除点位保持；专项测试、类型检查、ESLint、后端专项验证。

## 未完成
代码实现和隔离验证已完成；真实运行环境尚未重启验证。当前仓库完整类型检查与打包受已有问题阻塞，详见验证记录。

用户后续使用：重启原 scripts/start-thingsboard-video-local.ps1 提供新元数据，刷新 Vue 大屏，编辑 → 点位自定义 → 按设备配置。自定义配置可指定图标和数据/视频详情方式；检查旧样式冲突后保存模板。E:/simulationEquipment/start-all.ps1 接入流程未改。

上线前现场验收：实际配置改名/切换、图片权限、客户大屏、模型点位图标与视频播放。构建遗留问题应作为独立问题处理。

## 人工 Git 交付
建议先提交 runtime 元数据和后端测试，再提交前端配置展示/迁移及对应文档；建议消息：feat(map): 按设备配置统一点位图标与详情类型。未创建任何提交。AI 不执行暂存、提交、推送及 PR 操作。

