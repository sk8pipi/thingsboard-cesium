# 任务交付记录

## 基本信息

- 任务编号：agent-governance-ssot
- 任务名称：完善多 Agent 文件系统唯一真相来源
- 当前状态：待人工提交
- 当前主 Agent：主 Agent
- 最后更新：2026-08-08

## 协作登记

- 活动任务登记：docs/changes/active-tasks.json
- 任务目录：docs/changes/agent-governance-ssot
- 工作树：仓库主工作树
- 共享资源：无

## 目标

- 为活动任务、文件所有权和共享资源建立仓库内中央登记。
- 将任务决策、实现状态和验证事实落到可恢复的任务档案。
- 明确协作文档与 `.codex/agents/*.toml` 的规范源、执行投影关系。
- 提供无新增依赖的治理一致性校验脚本。

## 非目标

- 不修改业务代码、公共 API、数据库结构或部署拓扑。
- 不自动安装 Git Hook，不假设仓库使用特定 CI 平台。
- 不执行 Git 暂存、提交、推送或 GitHub 操作。

## 验收标准

- [x] 活动任务登记能够描述任务、状态、工作树、文件范围和共享资源。
- [x] 多 Agent 协作文档明确各类事实源、登记生命周期和单写入规则。
- [x] 任务模板包含协作登记、事实源影响和治理验证字段。
- [x] 校验脚本能够发现任务档案缺失、状态不一致、文件范围冲突、共享资源冲突和角色配置漂移。
- [x] 校验脚本在当前工作树通过。

## 必读文档

- AGENTS.md
- docs/ai/architecture-index.md
- docs/ai/project-architecture.md
- docs/ai/multi-agent-development.md

## 已批准方案摘要

- 用户批准补齐上一轮识别出的治理缺口。
- 使用仓库文件保存持久协作状态，不把聊天或 Agent 记忆作为权威来源。
- 中央活动登记由当前主 Agent 单点维护；业务实现 Agent 不修改该文件。
- 使用 PowerShell 校验脚本提供可在本地或任意 CI 中复用的机械检查。

## 工作包与文件所有权

| 工作包 | Agent | 文件范围 | 状态 | 停止条件 |
| --- | --- | --- | --- | --- |
| 治理规则与任务档案 | 主 Agent | `AGENTS.md`、`docs/ai/`、`docs/changes/` | 已完成 | 发现与既有架构或用户修改冲突 |
| 角色执行投影 | 主 Agent | `.codex/agents/*.toml` | 已完成 | 需要改变角色权限模型 |
| 治理校验 | 主 Agent | `scripts/validate-agent-governance.ps1` | 已完成 | 需要新增生产依赖或修改 Git 配置 |

## 当前进度

### 已完成

- 检查工作树、必读文档、任务模板和 `.codex` 角色配置。
- 建立中央活动任务登记和首个实际任务档案。
- 明确分类事实源、规范源和运行时执行投影。
- 更新四个基础 Agent 的启动上下文要求。
- 实现治理一致性校验脚本并通过正向与冲突负向测试。
- 完成最终差异和空白错误检查。

### 未完成

- 用户审核工作区差异并决定是否暂存和提交。

### 当前风险

- 中央活动任务登记仍依赖主 Agent 按规则更新；本次不安装自动 Git Hook。
- 不同 Git worktree 中的未提交登记不会自动实时同步，跨 worktree 启动任务仍需由中央主 Agent 串行登记。

### 下一步

- 用户审核差异后手动暂存和提交；提交完成后填写重要提交引用、将任务标记已完成并从活动登记移除。

## 修改范围

- 已修改文件：协作规范、架构索引、四个 Agent 执行配置、任务模板、活动登记、任务档案和治理校验脚本。
- 明确未修改：业务代码、API、数据库、基础设施配置和本地密钥。

## 事实源影响

- 新增正式来源：`docs/changes/active-tasks.json` 记录活动任务和资源占用。
- 长期任务事实：`docs/changes/<任务编号>/` 保存目标、决策、进度、交接和验证。
- 规范与执行关系：`docs/ai/multi-agent-development.md` 是规范源，`.codex/agents/*.toml` 是执行投影。
- 验证事实：`verification.md` 记录实际命令和结果，校验脚本提供机械证据。

## 人工 Git 交付

- 建议提交分组：多 Agent 治理规则、任务档案与校验脚本作为一个提交。
- 建议 commit message：`完善多 Agent 文件事实源与治理校验`
- 重要提交引用（由用户提交后填写）：
- AI Git 写操作：未执行
