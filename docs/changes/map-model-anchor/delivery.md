# 三维模型点位锚定

- 任务编号：map-model-anchor
- 当前状态：已归档（用户已提交）
- 当前主 Agent：主 Agent
- 最后更新：2026-09-09

## 目标与批准范围
完成第二期统一选点：传感器/摄像头新增与重新选点共用入口，命中模型跟随模型，命中地形解除模型锚点。确认候选只改草稿，顶部保存确认后直接同步 ThingsBoard 经纬度及绝对椭球高度，不备份旧位置。当前大屏移除不删除设备、不改变业务统计，支持恢复当前设备位置或重新选点恢复。第一期未提交修改全部保留。

## 验收标准
- 新增、重选、跨模型/地形切换、预览/取消/确认；错误模型、不可用深度不回退为地面。
- 移除清除地图点位和模型绑定，模板排除集合在刷新与资产切换后有效。
- 恢复重新读取设备权限/当前位置，不恢复旧锚点，不误写恢复前读值。
- 顶部保存前无设备写请求；多请求部分失败明确报告，待同步状态持久化，允许重试。
- 保存期间禁止重复操作及浏览器/站内路由切换；冻结目标 dashboard ID。
- 用户页和编辑页定位逻辑一致；移除前后部件统计口径不变。
- 模型变换与数据序列化有自动测试；真实 WebGL 和双页面最终显示需手动验收。

## 工作包与文件所有权
主 Agent 接回并独占全部写入；只读验证 Agent 审查跨层保存、排除、恢复与路由边界。独立审查指出的统计、恢复多余写回、保存期间路由风险已由主 Agent 修复并补回归测试。验证 Agent 后续额度不足，未进行修复后的独立复审。

范围：frontend/src/views/tb/map、frontend/tests、MapTemplateRuntimeService 与专项 JUnit、架构文档与任务记录。后端修改仅保证排除设备继续参与原有业务运行时采集，不改变公开 API、数据库、部署或既有 dashboard/租户授权边界；没有启停共享服务或写用户真实设备。

## 当前进度
2026-09-12：开始设备配置点位任务前检查工作树干净，HEAD 为用户提交 `bcceead 三维模型固定点位`。关闭旧活动登记；历史未执行的 WebGL 验收记录保持原样，不改写为通过。
2026-09-09 重新选点热修复：用户报告 attachPoint 的 structuredClone 无法复制 Vue Proxy。已用真实 Vue ref 重现相同调用链异常，将锚点及两层坐标记录复制为普通对象；补 reactive、readonly、嵌套 Proxy 和传感器/摄像头回归验证。未触碰用户另有的 .gitignore、启动脚本及 Angular UI 修改。
统一选点编辑页、排除/恢复、阶段化保存与持久化重试已完成。模型矩阵与拾取服务、用户页排除、业务设备引用和后端运行时采集已集成。八项前端测试脚本、后端专项测试、ESLint、治理校验和 diff 检查通过；定向类型检查无本次文件诊断，但关联链存在既有错误。真实 WebGL 手动验收仍未执行，详见 verification.md。

排除设备只保留类型及无坐标的业务引用，不能作为历史位置恢复；这用于兑现“不改统计”的批准要求。未增加跨模板设备唯一性限制。

## 下一步与风险
本机未发现 5173 前端监听，无法进行真实浏览器/WebGL 验收。用户启动更新后的前后端，按 verification.md 测试选点、保存、刷新、用户页、资产切换、模型变换和视频。
后端运行时本次有修改，生效需重新运行更新后的后端；只刷新前端不足以验证“隐藏不改统计”。
模型同 URL 内容替换需递增 revision；低 LOD/透明玻璃拾取有边界。保存跨 API 非事务；已成功部分不自动撤销，不备份旧坐标。
前端全项目类型检查仍有既存 14 个上游/关联组件诊断，本次文件无新增诊断。

## 非目标
跨模板唯一性约束、坐标历史备份、批量修改真实数据、新依赖、数据库迁移、部署改造、室内楼层/法线/视锥和测量级精度保证。

## 人工 Git 交付
AI 未执行暂存、提交、推送或 GitHub 操作。建议将前后端功能、测试、架构与本任务记录作为同一完整变更提交，避免先部署前端导致统计集合缩水。
建议 commit message：feat(map): 统一模型与地形选点并支持点位移除恢复。
