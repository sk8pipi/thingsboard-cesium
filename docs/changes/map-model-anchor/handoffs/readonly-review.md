# 只读调查与复核

Agent：anchor_data_audit。仅只读，未修改文件、启动服务或执行 Git 写操作。

## 数据调查
点位缓存白名单、设备坐标应用/写回、MapHome 设备和遥测合并均需保护新增字段；模板和后端 runtime JSON 为透传，无需新增后端字段或接口。

## 发现与处理
1. 普通选点对 Ref 对象直接判断导致提前返回：主 Agent 改为 .value，补实际 SFC 函数测试。
2. 从模板定位恢复设备位置后再次拖动被无条件排除写回：主 Agent 增加本次读回位置基线，覆盖仅恢复与随后拖动两条路径。

## 最终复核
未发现新增明确 P1。新增点位查询完成后检查取消/模式/位置变化；模型加载和拾取有代次保护，等价模型配置不随遥测重载；测试覆盖错误模型、深度失败、取消/卸载、图层恢复和能力检查。

真实 WebGL 表面精度、遮挡与保存后的双页面同步未验证，不用静态结果替代。
