# 项目 README 完善验证记录

## 验证范围

- README 内容、相对链接、图片文件、Mermaid、隐私与许可证说明。
- 多 Agent 章节与权威协作规范的一致性。
- Git 差异和治理登记。

## 治理一致性检查

| 命令 | 结果 | 备注 |
| --- | --- | --- |
| `powershell -ExecutionPolicy Bypass -File scripts/validate-agent-governance.ps1` | 通过 | 活动任务、任务档案和 Agent 配置符合治理规则 |

## 静态检查

| 命令 | 结果 | 备注 |
| --- | --- | --- |
| Markdown 链接与图片路径检查 | 通过 | 共检查 31 个 Markdown 链接，仓库内相对路径均存在 |
| Markdown 代码围栏检查 | 通过 | 共 32 个围栏标记，数量成对 |
| 图片尺寸与哈希检查 | 通过 | 两张目标图片与用户指定源文件 SHA-256 一致；尺寸为 1912×946、1905×934 |
| `git diff --check` | 通过 | 无空白错误；仅有 Git 的 LF→CRLF 工作区提示 |

## 未验证项与原因

- GitHub 线上渲染需要用户提交并推送后人工确认。

## 最终结论

- 通过。
- 说明：本地内容、图片与治理检查通过；GitHub 线上渲染需在用户提交并推送后确认。
