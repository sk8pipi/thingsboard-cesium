# 大屏资产层级选择交付记录

- 任务编号：map-asset-hierarchy
- 当前状态：已完成（用户提交 0be18af）
- 当前主 Agent：主 Agent
- 最后更新：2026-09-07

## 目标
按现有 COMMON/Contains 资产关系展示可展开、可搜索的资产树；任意层级选择继续筛选该资产及后代设备点位。

## 验收标准
- 楼栋、楼层及房间按真实关系显示，展开和选择分离。
- 搜索保留父级路径；恢复选择时展开父级。
- 完整分页目录、并发受控关系加载、刷新和短期缓存。
- 多父级、循环、不可访问实体、请求失败均有明确处理。
- 既有设备筛选、视频会话释放、客户用户权限及本地选择兼容。

## 范围
MapHome、MapAssetSelector、新增资产树工具和专项测试；不修改后端、数据库结构、管理端关系数据。

## 工作包与文件所有权
主 Agent 单独实施并验证，文件范围以 active-tasks.json 为准。

## 当前进度
已完成树目录、独立展开、任意层级选择、搜索保留路径、选中路径显示、刷新和关系缓存。多父级按路径展示，循环截断并提示，规模超限或加载失败保留上次完整结果。沿用现有后代资产点位筛选和权限范围。

## 下一步
用户刷新大屏后使用；楼层需通过现有资产管理功能建立从楼栋到楼层的 COMMON/Contains 关联。当前账号页面目录未显示楼层子资产，未为验证创建或修改实际关联。

## 人工 Git 交付
AI 不执行暂存、提交或推送。
