# 已批准决策

## 2026-09-25 设备控制部件基础适配

在用户已批准的控制/RPC/GPIO 优先范围内，将四种旧控制件和五种现代控制件接入声明式 `control` 家族，并将 `command_button` 按原生默认 `setState(true)` 接入 RPC Button 家族。Vue 保存状态读取方式、字段或读取 RPC、写入 RPC、超时/持久请求、数值范围、步长、精度、单位、标签及颜色；预览禁发 RPC，正式操作通过现有认证设备 RPC，失败恢复先前显示值。原生 JavaScript 转换函数不执行，现代部件可选的属性/遥测写入动作继续列为完整行为缺项。

RIO：风险为单向 RPC 已接收但设备业务未执行、持久 RPC 超时、状态字段与设备返回类型不一致；界面区分“请求已提交”和设备确认，仅在读取成功后更新真实状态。影响限前端配置、运行和现有 RPC/遥测读取，不改公共 API、权限、数据库或依赖。回退前保留新增模板 JSON；旧前端不识别 `control` 家族。

## 2026-09-25 实体与告警计数基础适配

在已批准的逐类 Vue 重写范围内，`entity_count` 与 `alarm_count` 使用现有认证的 `/api/entitiesQuery/count` 和 `/api/alarmsQuery/count`，按声明式条件查询；预览与正式展示同一组件，不执行原定义中的函数数据源或颜色脚本。点位入口以 `singleEntity` 锁定当前设备。RIO：筛选权限不足或查询失败时显示错误，不伪造 0；影响限于前端配置与只读 API 调用，无新依赖、公共接口、权限或数据库变更；回退前备份模板 JSON，旧前端不会识别新增 count 家族。原生全部筛选器、动作和造型仍待逐项验收。

## 2026-09-25 多属性更新基础适配

在用户已批准的逐类 Vue 重写范围内，`update_multiple_attributes` 仅通过既有认证 Telemetry API 按字段范围分组提交，并逐键回读；预览不会发送写入。保留原始定义和配置，拒绝执行原生 get/set 函数脚本。RIO：混合范围写入可能部分成功，界面显示部分失败及回读结果；影响限于前端配置和用户显式触发的写入，无新 API、权限、数据库或依赖；若回退，保留模板 JSON，旧前端不识别此运行家族。完整外观与高级脚本仍列为未适配。

## 2026-09-24 恢复原生全量适配

用户确认当前已适配效果符合预期，要求继续实现全部原生部件适配；先修部件库的临时优先级已结束。继续逐项对照仓库 ThingsBoard 源定义，保持 Vue 运行、原图、玻璃宿主、配置往返和待适配标记。当前先处理只读状态指示器（电池电量、信号强度）；控制型 status_widget 涉及 RPC，不误标为普通遥测部件。既有脚本不执行，扩展只在 version:1 可选配置内，不改公共 API、权限、数据库或生产依赖。完整一致性仍须逐定义验收，基础计数不等于完成。

## 2026-09-24 部件库优先修复

用户明确要求暂缓后续原生部件适配，先解决大屏添加部件时原生包和部件不显示的问题。仓库原始目录作为立即可浏览的基线；服务器系统包按 alias、部件按 fqn 补充，空数组、部分初始化或网络失败不得覆盖为零项。服务器完整定义仍在选择时校验，不把待适配部件当作可运行部件。影响限于前端浏览与本地测试夹具，不改后端 API、权限、数据库或生产依赖。回退可移除此合并逻辑，原目录投影与服务器资源均保留。

2026-09-25 用户明确将地图与位置 16 项、卡片/导航/平台入口 18 项排除出 Vue 产品范围，并要求部件库不显示；后端 ThingsBoard 原始定义继续保留用于兼容。统一按 FQN 在“所有部件”、包内列表和服务器合并入口过滤，同时隐藏过滤后为空的包。用户同时要求适配实体管理；资产/设备管理表复用 entityTable 家族及既有认证 API，新增、编辑、删除只能由显式操作触发，预览禁写，原始任意脚本不执行。其余未点名类别暂停扩展。

## 2026-09-15 原生流程与配置闭环扩展

用户在收到完整方案后明确要求实现。批准原生包分类和原图、左侧抽屉顶栏导入、基础/高级配置、真实预览与添加，并逐项接通配置的选择/保存/实际效果/重新编辑恢复。允许扩展前端配置模型及运行逻辑，保持现有后端 API、Vue 架构和显式保存入口。原生脚本不自动执行，不新增生产依赖、不批量迁移模板、不操作共享服务。

RIO：风险为原生图片与运行效果不一致、配置伪生效、旧配置丢失、跨入口保存不一致；通过逐类能力清单、共用 Renderer、可选增量字段和 JSON 往返测试控制。影响主要为原生选择器、配置器、数据运行与宿主入口；新增写操作业务部件只通过既有公开接口和显式用户动作。选择在 Vue 内逐类实现，拒绝执行导入脚本和将未知部件冒充折线图。回退前保留模板 JSON，原字段版本兼容；新增选项在旧前端可能忽略，不宣称回退后视觉一致。

2026-09-13 用户在完整讨论与缺项总结后要求实现，并明确大屏必须遵循液态玻璃效果。

- 以仓库原生 521 个定义为参照，按类别逐类适配；资源目录和 Vue 运行能力分层，未适配不可冒充成功。
- 共用部件目录、数据选择、配置与预览、运行组件；复用已有 Vue 注册表和遥测服务。
- 设备配置是筛选来源，不替代数据字段选择。点位详情自动绑定当前设备，资产自身与下属设备聚合需区分。
- 原生标识/配置与 Vue 适配版本显式关联，保留原始定义；未知类型禁止默认转折线图。任意 Angular/自定义脚本不自动运行。
- 大屏和点位详情的液态玻璃效果优先于原生不透明卡片外观；统一主题、透明绘图区和可调模糊。

## RIO
Risks：配置字段丢失、原生代码不能运行、预览与实际不一致、错误订阅、液态玻璃多层模糊拖慢。通过显式适配清单、保留原定义、同组件预览、校验及隔离测试控制。
Impact：Vue 部件管理、编辑、导入与运行扩展；沿用后端 API/鉴权/数据库和视频接口。无新生产依赖、无批量迁移。
Options：采用原生类别复用基础 Vue 组件与预设；不采用 Angular 内嵌冒充 Vue 重写，不自动执行导入代码，不以统一折线图替代全部部件。
回退：新配置增量保存、原始导入留存；不自动覆盖服务器系统部件或用户模板；旧版本不能盲写新部件配置。

## 2026-09-15 编辑交互修复

用户确认生成正常，要求能够调整部件大小且保存后不显示配置按钮。编辑态常显缩放手柄与配置入口，退出编辑后隐藏。首次主动缩放冻结当时画布行数，保存至现有 viewport 的 mode=fixed 与 rows，避免高度被自动铺满抵消；未缩放旧模板继续 fill。

RIO：风险是旧版前端不了解 fixed 会重新铺满；影响限于前端布局 JSON 解释，无后端 API、数据库或设备变更。采用显式 fixed 值保留旧 fill 兼容，优于全局改变所有旧模板的行数算法或仅在编辑期间冻结而保存后回弹。取消恢复现有草稿快照；回退代码前备份模板，旧版可能重新铺满，不能保证缩放外观。

## 2026-09-22 全量范围与聚合窗口补齐

用户连续要求“实现完全适配”，原始 521 个 JSON 外的 160 个安装生成 SCADA SVG 源纳入核对，总计 681 个源。基础适配与原生完整一致性分开记录，不把新增家族计数当作完整完成。

沿用已批准可选 version:1 字段策略增加 radial/range/aggregate 和 window.calendar。聚合使用现有认证遥测 API 的整窗聚合，日历/比较明确使用 UTC；不新增服务、依赖、公共 API、数据库或设备写操作。任意脚本仍不执行。完整别名/宿主时间窗口继承和业务写部件后续单独核对，不在当前声明支持。

RIO：风险为日历边界冻结、曲线截断污染聚合和比较失败扩散；采用每次计算窗口、服务端整窗查询、每位置独立结果及边界回归测试。影响仅前端配置与读取。旧无 calendar 字段行为保留，原始 rawSource 不改；旧版本忽略新增设置，回退前保留模板 JSON，不能保证旧前端还原新布局。

## 2026-09-23 液位类实现边界

在用户批准逐类适配范围内，新增 liquid 可选配置与十张原生 SVG 静态投影，保持现有最新值/属性 API、单实体单字段与原认证边界。属性绑定显式选择 CLIENT/SERVER/SHARED_SCOPE，不猜测跨范围优先级。颜色范围声明式计算，不执行原生 colorFunction；合法零值不复制上游真假值回退缺陷，提示值采用统一容量换算。原始 SVG 来源与许可记录在 nativeLiquidShapes.LICENSE.md；不接受用户导入 SVG 进入此运行器。

RIO：主要风险为单位错误、SVG ID 串扰、液位缺失伪装零与属性失败静默回退。用整套单位换算、独立 ID、空值/错误状态和单元/浏览器验证控制。影响只读前端渲染与配置增量，无后端/依赖/权限改变。取消和旧模板行为保持；旧前端不识别 liquid 家族，回退前备份并移除新部件。

## 2026-09-23 状态图与抽屉修复

在既有授权内新增 version:1 可选 state 字段与现代 state_chart 家族，不改公共 API/后端/生产依赖。窗口前状态通过既有认证历史接口的 limit=1 查询获取，强制 NONE 与严格类型；失败、未知文本和截断保持缺口，禁止伪造连续状态。风险为类型混淆及缺失区间错误续画，分别用严格映射与失败/截断回归控制。旧配置行为保留，旧版不识别该家族，回退前备份模板。

用户截图反馈后优先修复大屏原生与内置区域互相遮挡，限定为前端布局及验证夹具变更；维持原生/内置顺序和顶部导入入口。

## 2026-09-24 标量输入与旧版图表

继续沿用用户已批准的 Vue 逐类重写。旧版 Chart.js/Flot 中能按现有 Vue 图形家族声明式表达的图表使用原始 fqn 精确匹配；不执行原始控制脚本，Flot/Chart.js 特有高级选项仍标缺项。标量输入只开放 14 个明确的布尔、日期、整数、浮点、文本定义；原始 fqn 决定属性范围或遥测写入，单实体单字段，共享属性限设备。预览禁写，正式展示需显式提交，写入后回读并刷新运行数据；使用已有认证 Telemetry API 和后端权限，不新增接口、依赖或权限模型。复杂 JSON、图片、位置与多字段写入不套用标量表单。

RIO：风险为错误范围写入、预览误写、回读失败被误报成功、用户配置丢失。通过 fqn 白名单、配置和运行双重校验、预览禁写、写后回读、模拟传输测试与 JSON 往返控制。影响限于 Vue 目录、配置、运行和读取刷新；旧模板保持原样。回退前备份新部件模板 JSON，旧版前端不识别 input 家族；真实客户权限及设备写入仍需专门验收。

## 2026-09-24 JSON 输入

对原始 `input_widgets.update_json_attribute` 单独启用 JSON 对象/数组输入，复用已批准的 input 家族、认证 Telemetry API 和预览禁写链路。原始设置允许属性/遥测切换，属性允许服务端/共享范围；编辑目标变化时清空原字段并重新选择，运行时按配置重新核对字段类型、范围和共享设备限制。不把图片、位置或多字段输入归入该实现，也不运行原始 Angular 脚本。

RIO：风险为旧字段残留导致写错目标、无效 JSON、写入成功但未回读和重新编辑配置丢失；通过切换重选、对象/数组解析、写后回读、范围路由及 JSON 往返测试控制。影响仅限 Vue 配置与运行及生成目录，无接口/数据库/权限改变。旧模板保持原样；回退前保留新部件模板 JSON，真实客户权限与设备写入需单独验收。

## 2026-09-24 五类优先与风速风向

用户指定先完成输入/上传/认领、控制/RPC/GPIO、实体/告警、网关、仪表/风速风向五类；地图、静态导航与 SCADA 暂排其后。优先级不改变原生逐定义验收要求。风速风向两定义复用现有最新值读取与认证图片 API，新增声明式 wind 设置和 Vue SVG；不执行原生函数数据源、颜色脚本或卡片动作。首字段风向、第二字段可选风速，范围与背景配置随模板保存。

同日继续：RPC Button 只接一个 DEVICE 且无需字段，显式正式点击使用项目已有认证 RPC API；预览不产生设备副作用。Risks：双向超时与单向接收都不等于设备业务执行成功，原始持久化/轮询尚未实现。Impact：仅新增前端声明式设置/渲染和现有 API 调用，不修改后端权限或公共契约。Options：逐项补持久化及状态轮询，暂不把基础适配标为完整原生一致。罗盘复用 wind SVG，温度计复用垂直仪表；其专属原生刻度与造型仍单独验收。

RIO：风险为字段顺序混淆、缺风速显示成风向、背景图认证失败及刻度样式失效；通过字段数量/顺序提示、明确的缺值状态、图片失败回退和配置往返测试控制。影响仅前端目录/配置/渲染，不改公共 API、权限或数据库。回退前保留新增模板 JSON，旧前端不识别 wind 家族；真实设备与服务器图片仍需联调。

## 2026-09-25 控制、RPC 与 GPIO 基础适配完成

用户要求将该分类全部接入 Vue。12 个原始 fqn 精确映射到 advancedControl 家族，使用现有认证 RPC、持久 RPC 和 Telemetry 属性接口，不新增后端契约或依赖。预览统一禁止 RPC、属性写入、持久请求删除和链接跳转；正式运行仅执行声明式方法、参数、引脚或安全链接。原始 Angular/JavaScript 转换函数不执行。

RIO：风险为设备 RPC 方法不匹配、远程 Shell 命令副作用、持久请求状态差异及属性范围写错；通过原始默认方法/引脚预设、单设备无字段校验、显式按钮操作、范围校验和失败反馈控制。影响限 Vue 目录、配置器、运行器及现有 RPC 查询参数。回退前保留新增模板 JSON；真实设备固件、网关连接器和客户权限仍需集成验收，基础适配不计为完整原生一致性。
