# 已批准决策

## 2026-09-24 部件库优先修复

用户明确要求暂缓后续原生部件适配，先解决大屏添加部件时原生包和部件不显示的问题。仓库原始目录作为立即可浏览的基线；服务器系统包按 alias、部件按 fqn 补充，空数组、部分初始化或网络失败不得覆盖为零项。服务器完整定义仍在选择时校验，不把待适配部件当作可运行部件。影响限于前端浏览与本地测试夹具，不改后端 API、权限、数据库或生产依赖。回退可移除此合并逻辑，原目录投影与服务器资源均保留。

## 2026-09-15 原生流程与配置闭环扩展

用户在收到完整方案后明确要求实现。批准原生包分类和原图、左侧抽屉顶栏导入、基础/高级配置、真实预览与添加，并逐项接通配置的选择/保存/实际效果/重新编辑恢复。允许扩展前端配置模型及运行逻辑，保持现有后端 API、Vue 架构和显式保存入口。原生脚本不自动执行，不新增生产依赖、不批量迁移模板、不操作共享服务。

RIO：风险为原生图片与运行效果不一致、配置伪生效、旧配置丢失、跨入口保存不一致；通过逐类能力清单、共用 Renderer、可选增量字段和 JSON 往返测试控制。影响主要为原生选择器、配置器、数据运行与宿主入口；新增写操作业务部件只通过既有公开接口和显式用户动作。选择在 Vue 内逐类实现，拒绝执行导入脚本和将未知部件冒充折线图。回退前保留模板 JSON，原字段版本兼容；新增选项在旧前端可能忽略，不宣称回退后视觉一致。

2026-09-13 用户在完整讨论与缺项总结后要求实现，并明确大屏必须遵循液态玻璃效果。

- 以仓库原生 521 个定义为参照，按类别逐类适配；资源目录和 Vue 运行能力分层，未适配不可冒充成功。
- 共用部件目录、数据选择、配置与预览、运行组件；复用已有 Vue 注册表和遥测服务。
- 设备配置是筛选来源，不替代数据字段选择。点位详情自动绑定当前设备，资产自身与下属设备聚合需区分。
- 原生标识/配置与 Vue 适配版本显式关联，保留原始定义；未知类型禁止默认转折线图。任意 Angular/自定义脚本不自动运行。
- 大屏和点位详情的液态玻璃效果优先于原生不透明卡片外观；统一主题、透明绘图区和可调模糊。

## RIO
Risks：配置字段丢失、原生代码不能运行、预览与实际不一致、错误订阅、液态玻璃多层模糊拖慢。通过显式适配清单、保留原定义、同组件预览、校验及隔离测试控制。
Impact：Vue 部件管理、编辑、导入与运行扩展；沿用后端 API/鉴权/数据库和视频接口。无新生产依赖、无批量迁移。
Options：采用原生类别复用基础 Vue 组件与预设；不采用 Angular 内嵌冒充 Vue 重写，不自动执行导入代码，不以统一折线图替代全部部件。
回退：新配置增量保存、原始导入留存；不自动覆盖服务器系统部件或用户模板；旧版本不能盲写新部件配置。

## 2026-09-15 编辑交互修复

用户确认生成正常，要求能够调整部件大小且保存后不显示配置按钮。编辑态常显缩放手柄与配置入口，退出编辑后隐藏。首次主动缩放冻结当时画布行数，保存至现有 viewport 的 mode=fixed 与 rows，避免高度被自动铺满抵消；未缩放旧模板继续 fill。

RIO：风险是旧版前端不了解 fixed 会重新铺满；影响限于前端布局 JSON 解释，无后端 API、数据库或设备变更。采用显式 fixed 值保留旧 fill 兼容，优于全局改变所有旧模板的行数算法或仅在编辑期间冻结而保存后回弹。取消恢复现有草稿快照；回退代码前备份模板，旧版可能重新铺满，不能保证缩放外观。

## 2026-09-22 全量范围与聚合窗口补齐

用户连续要求“实现完全适配”，原始 521 个 JSON 外的 160 个安装生成 SCADA SVG 源纳入核对，总计 681 个源。基础适配与原生完整一致性分开记录，不把新增家族计数当作完整完成。

沿用已批准可选 version:1 字段策略增加 radial/range/aggregate 和 window.calendar。聚合使用现有认证遥测 API 的整窗聚合，日历/比较明确使用 UTC；不新增服务、依赖、公共 API、数据库或设备写操作。任意脚本仍不执行。完整别名/宿主时间窗口继承和业务写部件后续单独核对，不在当前声明支持。

RIO：风险为日历边界冻结、曲线截断污染聚合和比较失败扩散；采用每次计算窗口、服务端整窗查询、每位置独立结果及边界回归测试。影响仅前端配置与读取。旧无 calendar 字段行为保留，原始 rawSource 不改；旧版本忽略新增设置，回退前保留模板 JSON，不能保证旧前端还原新布局。

## 2026-09-23 液位类实现边界

在用户批准逐类适配范围内，新增 liquid 可选配置与十张原生 SVG 静态投影，保持现有最新值/属性 API、单实体单字段与原认证边界。属性绑定显式选择 CLIENT/SERVER/SHARED_SCOPE，不猜测跨范围优先级。颜色范围声明式计算，不执行原生 colorFunction；合法零值不复制上游真假值回退缺陷，提示值采用统一容量换算。原始 SVG 来源与许可记录在 nativeLiquidShapes.LICENSE.md；不接受用户导入 SVG 进入此运行器。

RIO：主要风险为单位错误、SVG ID 串扰、液位缺失伪装零与属性失败静默回退。用整套单位换算、独立 ID、空值/错误状态和单元/浏览器验证控制。影响只读前端渲染与配置增量，无后端/依赖/权限改变。取消和旧模板行为保持；旧前端不识别 liquid 家族，回退前备份并移除新部件。

## 2026-09-23 状态图与抽屉修复

在既有授权内新增 version:1 可选 state 字段与现代 state_chart 家族，不改公共 API/后端/生产依赖。窗口前状态通过既有认证历史接口的 limit=1 查询获取，强制 NONE 与严格类型；失败、未知文本和截断保持缺口，禁止伪造连续状态。风险为类型混淆及缺失区间错误续画，分别用严格映射与失败/截断回归控制。旧配置行为保留，旧版不识别该家族，回退前备份模板。

用户截图反馈后优先修复大屏原生与内置区域互相遮挡，限定为前端布局及验证夹具变更；维持原生/内置顺序和顶部导入入口。
