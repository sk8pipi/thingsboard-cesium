# 当前配置链路与验收范围

2026-09-23：本表记录当前代码提供的配置，不代表 ThingsBoard 所有配置已经复刻。正式运行边界见 docs/ai/vue-widget-library-architecture.md。

## 选择与添加

大屏编辑 → 添加部件 → 原生部件包 → 部件 → 基础/高级配置 → 预览 → 添加。导入按钮位于抽屉顶部，已导入资源单独进入；内置部件仍用原有缩略图及配置入口。普通仪表盘和点位详情复用原生两级浏览和同一配置器。

32 个原生包与部件归属由后端目录生成。服务器目录优先，系统目录投影回退有明确提示；尚未适配的条目显示待适配。原图是原始内嵌图片或现有认证图片 API 引用，不用生成图替代。

## 配置 → 保存 → 消费

| 适用范围 | 已连接的配置 | 保存字段 | 实际消费 |
| --- | --- | --- | --- |
| 共用 | 设备/资产、字段、属性范围、名称、颜色、单位、小数 | config.datasources | NativeWidgetDataCore 查询，Renderer/ChartOptions 格式与颜色 |
| 共用 | 标题及开关 | title、config.title/showTitle | Renderer 标题 |
| 共用 | 刷新间隔 | config.native.pollMs | 每轮完成后再轮询，固定历史图线不轮询；日历和含最新值的聚合卡继续刷新 |
| 共用 | 玻璃底色、边框、圆角、强调色 | appearance | 预览与 WidgetHost 共用玻璃样式；大屏全局设置优先 |
| 历史类 | 实时/固定/UTC 日历窗口、窗口长短、起止、聚合与间隔 | native.window | 历史查询与图表时间轴，滚动及日历轴每秒推进，日历起点跨期更新 |
| 数值/曲线/进度 | 名称/值/日期开关、布局、标签位置、字号、颜色、日期格式 | native.presentation、showLabel/showDate/fontSize | Renderer DOM 与日期计时 |
| 时序/数值曲线/标签柱图 | 每条序列线/柱/点、线宽、平滑、阶梯、点大小、面积、默认隐藏、数值标签、Y 轴 | dataKeys[].settings.native | ChartOptions；不适用的线型配置按序列类型隐藏 |
| 同上 | 坐标轴标题/左右/上下限、阈值线、堆叠、缩放、图例位置、提示框、动画 | native.chart | ECharts 配置；删除轴同步重新绑定；轮询保留交互状态 |
| 数值/进度/仪表/图表 | 数值颜色区间 | native.thresholds | 数值颜色、柱/点颜色及折线 visualMap |
| 进度 | 量程、方向、上下限、轨道色 | native.min/max/progress | Renderer 进度比例和轨道 |
| 仪表 | 圆/弧/条/温度计、方向、量程、刻度、指针、角度、宽度、名称/数值样式 | native.gauge、presentation、min/max | ChartOptions 仪表或线性量程图；角度/指针仅圆弧显示 |
| 饼/环图 | 内径、标签、百分比、排列方向、中心合计开关/标题/单位/精度/字号、图例/提示/动画 | native.pie、fontSize、chart、showLabel/showLegend | ChartOptions 饼图与中心合计，空数据不伪造零值 |
| 最新值柱图 | 横向/纵向、量程上下限、柱宽、分类标签、数值标签、图例/提示/动画 | native.latestBar、chart、showLabel/showLegend | ChartOptions 最新值柱图；只查询最新遥测或属性 |
| 雷达/极区 | 上下限、起始角度、分段、轴/刻度标签、雷达形状/归一化/线型/点形/填充、极区柱宽 | native.radial、chart | 雷达和极坐标柱图；雷达字段图例独立开关 |
| 范围图 | 单遥测字段、颜色区间、边界线、区间外颜色、填充透明度、缩放与图例 | native.range、thresholds、chart | 历史查询、分段着色和区间图例 |
| 聚合卡 | 一至五个位置、聚合、比较偏移/结果、单位/精度/字号/颜色/箭头、副标题、数值时间和曲线开关 | native.aggregate、showDate | 独立整窗服务端聚合、UTC 日历偏移和 Renderer 五位置布局 |
| 液位容器 | 十种原生 SVG、三布局、输入/容量/显示单位、容量、四个属性绑定及范围、四组颜色区间、底板、字体尺寸、液面动画、提示单位/精度/日期 | native.liquid、fontSize、presentation.dateFormat；主字段 decimals | 最新值 + 属性按范围读取，单位转换、原生液面裁剪与 Vue SVG，错误提示与空值占位 |
| 状态图 | 布尔/数字/文本常量、数值区间、状态名称与数值位置、窗口前状态、末尾延续、线宽/点/填充/默认隐藏/标签/Y 轴与缩放 | native.state、dataKeys[].settings.native、chart | 严格类型历史查询、声明式映射、阶梯图与每秒边界更新；仅 NONE，隐藏平滑/堆叠等不适用选项 |
| 历史表格 | 搜索、时间列、固定表头、分页、每页数、时间排序、日期格式 | native.table、presentation.dateFormat | nativeTableRows + Renderer 表格 |

每个编辑器先克隆草稿，预览再克隆快照；正式添加/应用发出完整 JSON。三个宿主沿用既有显式保存入口。新增配置是 version:1 的可选字段，缺失时补默认值，已有 false、0、null、空字符串、空数组和扩展字段保留。

## 已知边界

- 当前 521 个 JSON 定义中 405 项基础适配，共 15 个运行家族；剩余 116 项和安装生成的 160 个 SCADA SVG 源待适配。全量源清单共 681 项；基础适配数不是完整验收数。
- 不执行原始 Angular 脚本、函数数据源和后处理脚本。完整实体别名、仪表盘时间窗口继承、任意时区/全部日历类型、原生全部动作和高级布局尚未提供。
- 带标签柱图修正为历史语义；旧模板如果曾配置属性字段，重新编辑时需选择遥测与历史范围，原始配置不会自动删除。
- 前端回退不会理解新增展示选项，回退前备份模板 JSON；不能承诺回退后外观一致。没有数据库迁移或批量模板重写。
- 已跑证据仅见 verification.md。隔离夹具没有真实模板写入、客户权限或设备 API 验收；原图 API 引用需在 ThingsBoard 服务可用时验证。

液位字段行只提供实际消费的字段和精度；单位和颜色使用液位专用配置，不显示无效的通用设置。液位高级提示框样式、字体族/粗细及脚本动作仍未完整适配。
